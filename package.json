{
  "name": "pull-scan",
  "version": "0.0.2",
  "description": "Scan algorithm for pull streams",
  "main": "index.js",
  "scripts": {
    "preversion": "npm test",
    "postversion": "git push && git push --tags && npm publish",
    "test": "eslint **/*.js && tape test/*.js | tap-spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nichoth/pull-reduce-through.git"
  },
  "keywords": [
    "pull",
    "stream",
    "pull-stream",
    "reduce",
    "scan",
    "through",
    "transform"
  ],
  "author": "nichoth",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nichoth/pull-scan/issues"
  },
  "homepage": "https://github.com/nichoth/pull-scan#readme",
  "dependencies": {
    "pull-stream": "^3.4.5"
  },
  "devDependencies": {
    "eslint": "^3.6.1",
    "eslint-config-minlint": "^1.0.2",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
